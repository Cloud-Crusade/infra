# 대시보드 파일 프로비저닝 — json/ 하위의 대시보드 JSON 을 자동 로드
apiVersion: 1

providers:
  - name: loadtest
    type: file
    disableDeletion: false
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
      foldersFromFilesStructure: false
